package com.xnu.rocky.providers

/**
 * Hermes provider — minimal config for connecting to Hermes API.
 */
object HermesProviderConfig {
    const val DEFAULT_HOST = "http://localhost:8642/v1"
    const val DEFAULT_MODEL = "hermes"
}
